<div class="container">
  <div class="tag-results">
    <h2 id="tag-show-header">Showing Results For: </h2> <a href="/tags/<%= @tag.id %>" class="btn btn-lg btn-skin-border project-tags"><%= @tag.name %></a>
  </div>

  <%= render partial: "stub", collection: @projects, as: :project %>

</div>
