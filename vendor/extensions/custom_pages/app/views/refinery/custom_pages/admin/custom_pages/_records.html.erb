<%= render 'refinery/admin/search_header', :url => refinery.custom_pages_admin_custom_pages_path %>
<div class='pagination_container'>
  <% if @custom_pages.any? %>
    <%= render 'custom_pages' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
