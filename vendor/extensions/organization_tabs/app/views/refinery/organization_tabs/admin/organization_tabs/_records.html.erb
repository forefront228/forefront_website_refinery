<%= render 'refinery/admin/search_header', :url => refinery.organization_tabs_admin_organization_tabs_path %>
<div class='pagination_container'>
  <% if @organization_tabs.any? %>
    <%= render 'organization_tabs' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
